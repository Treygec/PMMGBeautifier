export class FilterLMButtons {
    let lms = document.querySelectorAll('[class=LocalMarket__container]')

    let adHeaders = lms.forEach( ad => { console.log(ad.querySelector('[class^=SectionHeader]'))})
}